//- Sidebar
mixin sidebarItem(href, icon, text)
  li.sidebar-item
    a.sidebar-link(href= href)
      ion-icon(name= icon)
      span= text
    .sidebar-animation

//- Filter
mixin filter()
  .filter
    .filter-select 
      select#filter-select
        option(value='all') All
        option(value='new') New
        option(value='sale') Sale
    .filter-search 
      input#filter-search(type='text' placeholder='Enter Keywords')
      button.button
        ion-icon(name='search')